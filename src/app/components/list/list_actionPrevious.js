import React from "react";

const ActionPrevious = ({ actions }) => {
  console.log("previous", actions, typeof actions);
  return <h1>A desenvolver {actions} </h1>;
};

export default ActionPrevious;
